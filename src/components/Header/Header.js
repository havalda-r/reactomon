import React from 'react';

import Navbar from '../Navbar/Navbar';

const header = () => {
  return (
    <header>
      <div>LOGO</div>
      <Navbar />
    </header>
  );
};

export default header;
